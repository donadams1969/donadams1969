name: Liveness
on:
  push:
    branches: [main]
  schedule:
    - cron: '*/10 * * * *'   # Pulses every 10 min forever
  workflow_dispatch:

jobs:
  pulse:
    runs-on: ubuntu-latest
    steps:
      - name: Genesis Node Pulse
        run: echo "⚔️ SAINT PAUL GENESIS NODE — ETERNALLY ALIVE ⚔️"
